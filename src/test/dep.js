/** See {@link USER} */

/**
 * Sum two numbers
 * @param {number} a first value
 * @param {number} b second value
 * @returns {number}
 */
function sum(a, b) {
  return a + b
}

function multiply(a, b) {
  return a * b
}
